# devlog 2026-05-06

22 commits + memory 갱신 2건. push 미진행 (local main = `8ba3eb3`, origin behind).

## 한 줄 요약

오전 — wiki retrieval 정합성 (P0/P1/F2/F3 + R1/R2/G7) 9건. 오후 — 분기 debate parity (Q-FIX-1/2) + admin 가시화 체인 (R3 wiki coverage → R4 comment trace → R5-A viewer → R5-B Obsidian graph). 마지막 — KIM TMF 듀레이션 진단 (코드 변경 0).

## 트랙별 정리

### A. Wiki retrieval 정합성 (오전)

외부 LLM 리뷰 packet 기반 4-step P0 + F2/F3 buggy edge case + R1/R2 운영도구 + G7 차단.

| 커밋 | 내용 |
|---|---|
| `b6eec0d` | wiki retriever 03_Assets/04_Funds 포함 (over-permissive — P0-1+3 에서 stage 분리로 reverse) |
| `789853c` | asset_coverage matcher 한국어 unicode `\b` issue → `re.ASCII` 강제. 금/대체 0→8, 크레딧 1→5 등. dead exclude code 제거 |
| `2ddfb66` | **P0-1+3** stage/fund_code gating. `STAGE_ALLOWED_DIRS` dict — `_market/market_debate` 에서 04_Funds 차단, fund_comment 에서만 fund_code exact match 통과 |
| `20db0ad` | **P0-2** period filter (frontmatter / filename / period_agnostic dirs allowlist). 회귀: 4월 _market 에서 future page 21건 차단 |
| `cb84b4d` | **P0-4** cluster cap (default=2, frontmatter event_group_id 우선 / top_topics[0] fallback). Iran/oil 도배 → 다양화 |
| `3377a0c` | **F2** pinned_fund_context 별도 role + fund-specific keyword. 자기 04_Funds 페이지가 prompt "## 펀드별 Wiki 메모 (pinned)" 섹션 contract |
| `6fd22e8` | **F2 follow-up** retrieve `exclude_paths` — pinned 가 retrieved 에 또 등장하는 중복 dedup |
| `696c350` | **F3 P1.5-b** `_event_group_id` sequential→deterministic MD5(article_id sorted) hex10. 재실행 일관 |
| `9071da5` | **F3 P1.5-a** `refresh_base_pages_after_refine()` 시작 시 `01_Events/{month}_event_*.md` 좁은 범위 wipe. 02/03/04/05 dir 절대 안 건드림 |
| `37e2dbf` | **enrichment 보호** — 4월 wiki replay 가 P3-4/5 enrichment 5개 (07G04/08K88/국내주식/해외주식/환율) 를 base draft 로 덮어쓴 incident → `git checkout eae5491 --` 즉시 복원 + `_is_enrichment_page()` guard (frontmatter `source_type=asset_wiki/fund_wiki` 검출 시 skip) |
| `9d3d7ce` | **R1** `tools/wiki_retrieval_coverage.py` (CLI, 597 LOC, 4 섹션) |
| `6f9f908` | **R2** gate 모드 (`--fail-on-gate` + 7 gate G1~G7). skip period policy. R2 운영 결과: FAIL=0 / WARNING=2 (G7 stale month leakage 만) |
| `07a2812` | **R2 G7** fund_comment STAGE_ALLOWED_DIRS 에서 04_Funds 자체 제거 → pinned 단일 진입 경로. G7 WARN→FAIL 강화. 회귀 6 cases 신설 |

오전 중 incident 복구 1건 (enrichment overwrite) — 근본 fix `_is_enrichment_page()` guard 박힘.

### B. 분기 debate parity (오후 초반)

08N81 1Q quarterly draft 산출 후 발견된 PA empty / debate trace empty 이슈 → small fix 2건.

| 커밋 | 내용 |
|---|---|
| `0ac9071` | **Q-FIX-1** `run_quarterly_debate` 가 `_debug_trace` / `evidence_annotations` 미생성 → last_ctx 보존 + 합성 + helper `_evidence_month_distribution`. monthly 와 parity. 회귀 6 cases |
| `45c88e1` | **Q-FIX-2** `fund_comment_service` 가 outdated PA schema 키 (`pa_by_class`/`fund_return`) 를 사용 — `compute_single_port_pa` 는 `asset_summary` DataFrame 반환 → adapter 신설 (decimal × 100 % 단위 변환). 회귀 7 cases |

08N81 1Q draft 는 재생성 X (사용자 가이드).

### C. Admin 운영 가시화 체인 (오후 후반)

R3 → R4 → R5-A → R5-B. Comment trace 의 backend 생성 → API 노출 → React viewer → Obsidian graph 까지.

| 커밋 | 내용 |
|---|---|
| `5dd1d81` | **R3** `tools/wiki_retrieval_coverage.py` JSON output (16 top-keys, schema 1.0.0). FastAPI `/admin/wiki-coverage/{reports,latest,{report_id}}` 3 endpoint. payload=dict 진화 호환. path traversal 2층 방어 |
| `5aa2cde` | **R3 small fix** fail-on-gate FAIL fixture 4 cases (monkeypatch 로 fake inventory/retrieval/coverage) |
| `793db1b` | **R4** `tools/comment_trace.py` (~430 LOC, schema 1.0.0). 9 node type / 8 edge type. find_market_source 4-tier (path/run_id/period_fallback/none, confidence high/medium/low/none). attribution_method (explicit_ref/section_default). evidence_annotations 합성 fallback. FastAPI `/admin/comment-trace{,/latest,/{trace_id}}` 3 endpoint. 회귀 18 cases |
| `e60a713` | **R5-A** AdminTab Comment Trace Viewer (read-only, ~330 LOC). selector / list / summary card (confidence pill) / section attribution table / source groups / graph_seed type counts / raw JSON. graph viz 없음 (R5-B 분리). 4번째 sub-view 등록. tsc 0 / build 29.94s |
| `8ba3eb3` | **R5-B** Obsidian-like graph view. cytoscape ^3.33.3 + react-cytoscapejs ^2.0.0 + @types/cytoscape. force-directed `cose` layout. node 9 type + edge 8 type stylesheet selector (색/모양/크기 차별화). 체크박스 nodes/edges 필터 (evidence edges 기본 off, 45/68 → 23 visible). reset layout / show-hide. 클릭 detail panel (warning 시 강조). empty/error state. 540px height dark bg. tsc 0 / build 19.65s |

### D. 운영 정책 / 진단

| 항목 | 내용 |
|---|---|
| `1781862` | wiki weekly batch commit policy 신설. `tools/weekly_wiki_commit.py` (idempotent, no-op or `chore(wiki): weekly batch (catchup={N}d, files={F})`). CLAUDE.md 룰 (세션 시작 때 7일 임계 + 미커밋 변경분 점검 → 사용자 ask). memory `reference_wiki_commit_policy.md` |
| `bd118e4` | wiki weekly batch 첫 catchup commit (catchup=2d, files=24) |
| (코드 변경 0) | **KIM TMF 듀레이션 진단**. 사용자 발견 — KIM 페이지 카드 (0.75/3.18, 4-22 기준) ≠ fetcher priceList[0] (0.7098/3.1625, 5-05 기준). 추적 → KIM Next.js page chunk JSX 안에 정적 하드코딩 (build hash `f5ea45edb7ca72db.js`, buildId `20260429144235`). chunk 내 papi.kitmc URL 0개. 사용자 결정 **옵션 A — priceList[0] 유지**. memory `reference_kim_page_static_duration.md` |

## 메모리 갱신

- `reference_kim_page_static_duration.md` 신규 (KIM 정적 카드 vs 일별 fetcher, 옵션 A 결정)
- `reference_wiki_commit_policy.md` 신규 (wiki weekly batch + 7일 임계 + 세션 시작 ask)
- `feedback_review_packet_format.md` 신규 (외부 LLM 리뷰 packet 형식 룰: small fix 6 / 큰 변경 12 / Reviewer Q / 다음 우선순위)

## 신규/변경 파일 (요약)

```
modules/                                       (변경 0 — 듀레이션 fetcher 정상)
market_research/report/wiki_retriever.py       STAGE_ALLOWED_DIRS / period filter / cluster_cap / exclude_paths / pinned ctx / fund kw
market_research/report/debate_engine.py        run_quarterly_debate parity (last_ctx 보존 + _debug_trace + evidence_annotations 합성)
market_research/report/fund_comment_service.py _adapt_compute_single_port_pa (Q-FIX-2)
market_research/core/dedupe.py                 _stable_event_group_id() (P1.5-b)
market_research/wiki/draft_pages.py            wipe + _is_enrichment_page guard (P1.5-a + enrichment 보호)
market_research/core/salience.py               asset_coverage matcher hotfix (re.ASCII / dead code 제거)

tools/wiki_retrieval_coverage.py               R1+R2+R3 (markdown + JSON + 7 gate)
tools/comment_trace.py                         R4 신규 (~430 LOC)
tools/weekly_wiki_commit.py                    wiki weekly batch idempotent

api/services/wiki_coverage_gateway.py          R3 read-only loader (path traversal 2층)
api/services/comment_trace_gateway.py          R4 read-only loader (PERIOD_RE/FUND_RE/TRACE_ID_RE)
api/schemas/wiki_coverage.py                   payload=dict[str, Any] 진화 호환
api/schemas/comment_trace.py                   동일 패턴
api/routers/admin.py                           R3 3 + R4 3 = 6 endpoint 추가 (총 10)

web/package.json                               cytoscape ^3.33.3 / react-cytoscapejs ^2.0.0 / @types/cytoscape
web/src/api/generated/openapi.d.ts             재생성 (paths 14→20, app.openapi() 직접 호출 — Windows zombie 회피)
web/src/api/endpoints.ts                       WikiCoverage*/CommentTrace* DTO alias + 6 fetcher
web/src/hooks/useCommentTrace.ts               신규 (useCommentTraceList/Latest/ById)
web/src/tabs/AdminCommentTracePanel.tsx        R5-A 신규 (~330 LOC) + R5-B graph 통합
web/src/tabs/AdminCommentTraceGraph.tsx        R5-B 신규 (~467 LOC, cytoscape Obsidian-like)
web/src/tabs/AdminTab.tsx                      4번째 sub-view "comment-trace"
web/src/types/react-cytoscapejs.d.ts           모듈 선언 (upstream types 부재)

market_research/tests/test_*                   회귀 신규 7 파일 (총 55+ cases)
debug/probe_kim*.py                            KIM endpoint/chunk 진단 (보존 — 향후 재추적 시 재사용)
debug/comment_trace/2026-Q1/08N81.json         comment_trace 1차 산출물 (31 nodes / 68 edges)
```

## 검증 / 회귀

- `test_dedupe_deterministic_event_id.py` (3) — F3 P1.5-b
- `test_refresh_event_page_idempotent.py` (3) — F3 P1.5-a
- `test_fund_comment_excludes_04_funds.py` (6) — G7
- `test_quarterly_debate_parity.py` (6) — Q-FIX-1
- `test_pa_schema_adapter.py` (7) — Q-FIX-2
- `test_wiki_coverage_json_and_gate.py` (12) — R3 (4 small fix 포함)
- `test_comment_trace.py` (18) — R4
- 누계 55/55 PASS

R2 gate 운영 결과: FAIL=0 / WARNING=2 (G7 stale month leakage — 5월 fund_comment 의 retrieve 에 4월 자기 fund page 등장. STAGE_ALLOWED_DIRS 차단 후 0)

R5-A/B 빌드: tsc 0 errors / vite build PASS (5.5MB chunk warning 은 plotly 부터 기존, cytoscape 추가 영향 +~150KB)

## 변경 안 한 것 (사용자 가이드 일관 유지)

- `report_output/{period}/{fund}.{draft,final}.json` — 0
- approved final / debate output 본문 — 0
- 08N81 1Q draft 재생성 — X
- LLM 호출 — 0
- P2 edge store / causal path prompt — X
- F1 super-cluster (top_topics[0] proxy 작동 중) — X
- comment generation [ref:N] 도입 — X
- sentence-level attribution — X
- KIM fetcher 코드 — 0 (옵션 A 확정 후에도 동작 동일)

## 미해결 / Caveat

- **live HTTP smoke** 미실행 — Windows zombie socket :8000 PID 20328. 코드 blocker 아님 (`app.openapi()` 직접 호출로 OpenAPI 재생성 우회). 환경 reboot 후 가능
- **comment trace 한계** — 현재 graph 는 section-level. 08N81 1Q draft 처럼 explicit_ref=0 인 코멘트는 모든 section→evidence 가 default fanout → 시각적으로 1 section 이 모든 evidence 에 동일 연결된 것처럼 표시. 의미 분기는 [ref:N] 도입 후 가능
- **KIM 페이지 카드 vs fetcher 차이** — 옵션 A 정책 메모리 박힘. 신규 KIM 펀드 추가 시 동일 정책 적용
- **R5-A close 인정 받았으나 R5-B 는 사용자 close 아직 (이 세션 마지막에 KIM 진단 → 사용자 종료)**

## 다음 세션 우선순위

| 후보 | 추천 | 비고 |
|---|---|---|
| **comment generation [ref:N] 도입** | 🟢 1순위 | `fund_comment_service.generate_fund_comment_and_save` prompt 가이드 한 줄. 다음 사이클부터 explicit_ref > 0 발생, R5-B graph 가 의미 있게 분기 |
| **R5-A/B close 인정 + push** | 🟢 | 사용자 R5-A close 인정 받음. R5-B 미인정 — 사용자 확인 후 origin push |
| **08N81 1Q final 승인 / 또는 재실행 정책** | 🟡 | draft 만 있음. 사용자 결정 |
| **sentence-level attribution** | 🟡 후속 | comment_trace schema 진화 — 새 node type `comment_sentence` + ref 매칭 |
| **wiki_retriever 03/04 포함 후속** | 🟡 | 5/6 _market 재실행에서 03/04 점수 낮아 미선정. 다른 키워드 조합에선 등장 가능. 실 운영 신호 후 재평가 |
| **bundle 최적화 (manualChunks)** | 🟡 옵션 | 5MB chunk 경고 해소 |
| **Windows zombie cleanup + live HTTP smoke** | 🟡 환경 | reboot 후 R3+R4+R5-A 6 endpoint 1회 실측 |
| **pytest 격리 마이그레이션** | 🟡 | `test_fund_comment_excludes_04_funds.py` 운영 wiki 의존 → tmp_path + monkeypatch |
| **P2 edge store / causal path** | 🔴 보류 (사용자 지시) | |
| **F1 super-cluster** | 🔴 보류 | top_topics[0] proxy 작동 중 |
| **5월 운용보고 사이클** | 🔴 skip 유지 | |

## 시작 프롬프트 (다음 세션)

```
memory/handoff_react_fastapi_refactor.md + devlog/2026-05-06.md 확인하고 이어서 작업해.
오늘 마지막 작업: R5-B Obsidian graph (commit 8ba3eb3, R5-A close 인정 / R5-B 미인정), KIM TMF duration 진단 (옵션 A 메모리 박힘).
```
